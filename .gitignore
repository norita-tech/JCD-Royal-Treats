# Dependencies
node_modules/
server/node_modules/

# Environment variables — NEVER commit this (contains your password)
.env
server/.env

# Persistent data directory (database + uploaded images)
server/data/

# SQLite database files (anywhere in server/)
server/*.db
server/*.db-shm
server/*.db-wal

# OS files
.DS_Store
Thumbs.db
desktop.ini
