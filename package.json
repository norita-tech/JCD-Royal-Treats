{
  "name": "jcd-royal-treats",
  "version": "1.0.0",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node server/server.js",
    "postinstall": "npm install --prefix server"
  }
}
